# ====== n8n ======
# ถ้าเปิดพอร์ตตรง ให้ใช้ PROTOCOL=http และ HOST=IP-สาธารณะ:5678
# ถ้าอยู่หลังพร็อกซี (HTTPS) ให้ใช้ PROTOCOL=https และ HOST=โดเมนจริง (เช่น n8n.example.com)
N8N_PROTOCOL=http
N8N_HOST=localhost
N8N_PORT=5678
GENERIC_TIMEZONE=Asia/Bangkok
TZ=Asia/Bangkok

# คีย์เข้ารหัส credential ของ n8n — สร้างด้วย:  openssl rand -hex 32
N8N_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef

# ====== Postgres ======
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=postgresql
DB_POSTGRESDB_PORT=5432
DB_POSTGRESDB_DATABASE=n8n
DB_POSTGRESDB_SCHEMA=public
DB_POSTGRESDB_USER=n8n_user
DB_POSTGRESDB_PASSWORD=password

POSTGRES_USER=n8n_user
POSTGRES_PASSWORD=password
POSTGRES_DB=n8n